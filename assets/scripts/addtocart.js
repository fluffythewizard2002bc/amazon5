/*let count = document.querySelector('.count');const inc = document.querySelector('.inc');
const increment = () => {
  count.textContent++;
  document.body.appendChild(randomElement)
};
inc.addEventListener('click', increment);document.querySelector('.dec').addEventListener('click', () => {
  +count.textContent === 0 ?
  (count.textContent = 0) :
    count.textContent--;    randomElement.innerHTML = `<h1>Bye</h1>`
});
const randomElement = document.createElement('p');
randomElement.innerHTML = `<h1>hi again</h1>`
console.log(randomElement)*/